import { ConsoleLogger } from '@nestjs/common';

/**
 * Advanced logger for customizing logging
 *
 * @exports AdvancedLogger
 * @extends ConsoleLogger
 */
export class AdvancedLogger extends ConsoleLogger {
  // override methods if needed
}
